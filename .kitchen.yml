---
driver:
  name: vagrant

provisioner:
  name: chef_solo

platforms:
  - name: ubuntu-12.04

suites:
  - name: default
    run_list:
      #- recipe[minitest-handler]
      - recipe[thumbor_test::default]
    attributes: {}
  - name: source
    run_list:
      #- recipe[minitest-handler]
      - recipe[thumbor_test::source]
    attributes: {}
